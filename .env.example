# Copy this file to .env before using docker compose.
PANEL_PORT=8080
PALWORLD_PORT=8211
PALWORLD_QUERY_PORT=27015
RCON_PORT=25575

# Change these before exposing the server outside your private network.
RCON_PASSWORD=change-me-rcon-password
PANEL_SECRET_KEY=change-me-panel-secret-key

# Internal-only Palworld REST API for accurate player lists. Do not add a
# Docker host port mapping for this service.
PALWORLD_REST_ENABLED=false
PALWORLD_REST_PORT=8212
PALWORLD_REST_PATH=/v1/api/players
PALWORLD_REST_USERNAME=admin
# Leave blank to reuse RCON_PASSWORD. Set only when your server needs a different REST credential.
PALWORLD_REST_PASSWORD=

PALWORLD_CONTAINER_NAME=palworld-panel-palworld-1
PANEL_CONTAINER_NAME=palworld-panel
PALWORLD_APP_ID=2394010

SERVER_NAME=Palworld Docker Server
SERVER_DESCRIPTION=Managed by Palworld Panel
SERVER_MAX_PLAYERS=32

PANEL_ASSET_VERSION=docker

# Optional: let the Palworld container use a proxy running on the Docker host.
# Clash Verge Rev's common mixed port is 7897. Enable "Allow LAN" in the GUI
# if the container cannot reach the proxy.
# HTTP_PROXY=http://host.docker.internal:7897
# HTTPS_PROXY=http://host.docker.internal:7897
# ALL_PROXY=socks5://host.docker.internal:7897
# NO_PROXY=localhost,127.0.0.1,palworld,panel,host.docker.internal

# SteamCMD uses direct networking first, then the proxy on the next retry.
# Use direct or proxy to force one route while troubleshooting.
STEAMCMD_NETWORK_MODE=auto

# MOD manager
MOD_LIBRARY_ROOT=/app/data/mod-library
MOD_UPLOAD_MAX_BYTES=1073741824
PALWORLD_MOD_MODE=pak-safe
